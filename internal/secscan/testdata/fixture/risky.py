import os
import subprocess
import requests

eval("print(1)")
exec("x = 1")
subprocess.run("id", shell=True)
os.system("ls")
requests.get("https://example.com")
os.environ["KEY"]
open(".env")
# bind
host = "0.0.0.0"
